@model List<int>
@{
    ViewData["Title"] = "Index";
}

<h1>一百万条数据遍历用时： @ViewBag.time</h1>
<table>
    @for (int i = 0; i < Model.Count; i++)
    {
        <tr>
            <td>@i.ToString()</td>
        </tr>
    }
</table>

